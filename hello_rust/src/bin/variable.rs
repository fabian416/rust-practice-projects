#[allow(unused)]

fn main() {
    let x: i32 = -123;

    // x += 4;

    let mut y = 24;
    y += 1;

    // default type is signed integer i32
    let z = -123;
    const NUM: u32 = 1;

    // Re declare a variable is called shadowin
    let x: i32 = -1;
    let x: bool = true;

    let x: Vec<_> = vec![1, 2, 3];
}
